# PM2-Monitor

This script allows you to monitor the restart counter of one or multiple apps that are running in pm2.

# Usage

Rename the `.env.example` to `.env` and edit the data. The variable `MAIL_FROM` is currently also used as the auth mail.